# Frontend environment variables (Vite)
# VITE_ prefix is required for Vite to expose these to the browser bundle.
# Do NOT put secrets here — this file is bundled into the client.

VITE_API_BASE_URL=
